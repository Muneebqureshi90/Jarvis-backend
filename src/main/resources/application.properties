# Server Configuration
server.port=8080
server.servlet.context-path=/api

# H2 In-Memory Database (for demo)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# H2 Console (enabled for debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Prevent Jackson from serializing lazy-loaded entities prematurely
spring.jackson.serialization.indent-output=true

# Alternative MySQL Configuration (uncomment and update for production)
# spring.datasource.url=jdbc:mysql://localhost:3306/jarvis_db?useSSL=false&serverTimezone=UTC
# spring.datasource.username=${DB_USERNAME:root}
# spring.datasource.password=${DB_PASSWORD:}
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto=update
